public enum RentalStatus {
	requested, processed, readyForApproval, approved, cancelled, ordered, refused, accepted, submittedForPayment
//deze moet in bepaalde volgorde, is dit ook effectief zo bij enum: want je kan niet approved hebben na ordered etc
}
